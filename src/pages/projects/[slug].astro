---
import Layout from '../../layouts/Layout.astro';
import { projects } from '../../data/projects';

import paydartBanner from '../../assets/paydart__banner.png';
import pregtrackBanner from '../../assets/Pregtrack_banner.png';
import famconnectBanner from '../../assets/famconnect__banner.png';
import staffezeeBanner from '../../assets/staffezee_banner.png';
import pharmavaultBanner from '../../assets/Pharamavault__banner.png';

const bannerMap: Record<string, ImageMetadata> = {
  'paydart': paydartBanner,
  'pregtrack-app': pregtrackBanner,
  'famconnect': famconnectBanner,
  'staffezee': staffezeeBanner,
  'pharmavault': pharmavaultBanner,
};

const publicProjectSlugs = ['paydart', 'pregtrack-app', 'famconnect', 'staffezee', 'pharmavault'];
const publicProjects = projects.filter((project) => publicProjectSlugs.includes(project.slug));

export function getStaticPaths() {
  const publicProjectSlugs = ['paydart', 'pregtrack-app', 'famconnect', 'staffezee', 'pharmavault'];
  return projects.filter((project) => publicProjectSlugs.includes(project.slug)).map((project) => ({ params: { slug: project.slug } }));
}

const { slug } = Astro.params;
const project = publicProjects.find((item) => item.slug === slug);
if (!project) return Astro.redirect('/');

const currentIndex = publicProjects.findIndex((item) => item.slug === slug);
const prevProject = publicProjects[currentIndex - 1] ?? null;
const nextProject = publicProjects[currentIndex + 1] ?? null;
const banner = slug ? bannerMap[slug] ?? null : null;
---

<Layout 
  title={`${project.title} | ${project.type} Case Study - Akshayan Mohandass`}
  description={project.desc}
  ogImage="/final_state.jpg"
  ogType="article"
  keywords={`${project.title}, ${project.type}, ${project.tools.join(', ')}, ${project.tags.join(', ')}, Akshayan Mohandass, Product Design Case Study`}
>
  <div class="case-study">
    <a class="cs-skip" href="#cs-overview">Skip to case study</a>

      <!--  TOP NAV  -->
      <nav class="cs-topnav" id="cs-topnav" aria-label="Case study navigation">
      <div class="cs-topnav-inner">
        <a href="/#projects" class="cs-back">Back to all works</a>
        <span class="cs-breadcrumb mono">
          <a href="/">Home</a> / <a href="/#projects">Works</a> / {project.title}
        </span>
      </div>
    </nav>

    <!--  HERO  -->
    <header class="cs-hero" id="cs-hero" style={`--accent: ${project.accent}; --accent-dark: ${project.accentDark}`}>
      <div class="cs-container">
        <span class="cs-type-badge mono">{project.type}</span>
        <h1 class="cs-title">{project.title}</h1>
        <p class="cs-tagline">{project.desc}</p>

        <div class="cs-meta-strip">
          <div class="cs-meta-item">
            <span class="cmi-label mono">ROLE</span>
            <span class="cmi-val">{project.role}</span>
          </div>
          <div class="cs-meta-item">
            <span class="cmi-label mono">COMPANY</span>
            <span class="cmi-val">{project.company}</span>
          </div>
          <div class="cs-meta-item">
            <span class="cmi-label mono">DURATION</span>
            <span class="cmi-val">{project.duration}</span>
          </div>
          <div class="cs-meta-item">
            <span class="cmi-label mono">YEAR</span>
            <span class="cmi-val">{project.year}</span>
          </div>
          <div class="cs-meta-item">
            <span class="cmi-label mono">TOOLS</span>
            <span class="cmi-val">{project.tools.join(' | ')}</span>
          </div>
        </div>

        <div class="cs-executive-summary" aria-label="Project snapshot">
          <div><span class="mono">MY ROLE</span><strong>{project.role}</strong></div>
          <div><span class="mono">TEAM</span><strong>{project.team}</strong></div>
          <div><span class="mono">EVIDENCE</span><strong>{project.outcomes[0]?.metric} {project.outcomes[0]?.label}</strong></div>
        </div>
      </div>
    </header>

    <!--  HERO VISUAL  -->
    <div class="cs-hero-visual" style={`--accent: ${project.accent}`}>
      <div class="cs-container">
        <div class="cs-cover-placeholder">
          <div class="cs-cover-browser">
            <div class="cs-cover-bar">
              <span class="dot red"></span>
              <span class="dot yellow"></span>
              <span class="dot green"></span>
              <span class="cs-cover-url mono">{project.company.toLowerCase().replace(/\s/g, '')}.design / {project.slug}</span>
            </div>
            <div class={`cs-cover-screen ${banner ? 'has-banner' : ''}`}>
              {banner ? (
                <img src={banner.src} alt={`${project.title} banner`} class="cs-cover-banner-img" loading="eager" />
              ) : (
                <span class="cs-cover-label mono">{project.title.toUpperCase()}<br/>- PROJECT VISUALS -<br/>COMING SOON</span>
              )}
            </div>
          </div>
        </div>
      </div>
    </div>

    <nav class="cs-case-toc" aria-label="Case study sections">
      <div class="cs-container">
        <a href="#cs-overview">Overview</a><a href="#cs-challenge">Challenge</a><a href="#cs-research">Research</a><a href="#cs-process">Process</a><a href="#cs-screens">Solution</a><a href="#cs-decisions">Decisions</a><a href="#cs-outcomes">Outcomes</a><a href="#cs-learnings">Learnings</a>
      </div>
    </nav>

    <main class="cs-container cs-body">

      <!--  OVERVIEW  -->
      <section class="cs-section" id="cs-overview">
        <div class="cs-overview-grid">
          <div class="cs-overview-left">
            <h2 class="cs-section-label mono">OVERVIEW</h2>
            <p class="cs-overview-text">{project.overview}</p>
          </div>
          <div class="cs-overview-right">
            {project.outcomes.slice(0, 1).map(o => (
              <div class="cs-stat-hero" style={`--accent: ${project.accent}`}>
                <span class="cs-stat-num">{o.metric}</span>
                <span class="cs-stat-label mono">{o.label}</span>
              </div>
            ))}
            <div class="cs-team-card">
              <span class="cmi-label mono">TEAM</span>
              <span class="cs-team-val">{project.team}</span>
            </div>
          </div>
        </div>
      </section>

      <!--  THE CHALLENGE  -->
      <section class="cs-section" id="cs-challenge">
        <h2 class="cs-section-label mono">THE CHALLENGE</h2>
        <div class="cs-challenge-grid">
          <div class="cs-challenge-left">
            <p class="cs-challenge-text">{project.problem}</p>
          </div>
          <div class="cs-challenge-right">
            <div class="cs-pain-list">
              <div class="cs-pain-header mono">PAIN POINTS IDENTIFIED</div>
              {project.painPoints.map((p, i) => (
                <div class="cs-pain-item">
                  <span class="cs-pain-num mono">{String(i + 1).padStart(2, '0')}</span>
                  <span class="cs-pain-text">{p}</span>
                </div>
              ))}
            </div>
          </div>
        </div>
      </section>

      <!--  RESEARCH & DISCOVERY  -->
      <section class="cs-section" id="cs-research">
        <h2 class="cs-section-label mono">RESEARCH & DISCOVERY</h2>
        <div class="cs-research-grid">
          {project.researchMethods.map((m, i) => (
            <div class="cs-research-card" style={`--delay: ${i * 0.08}s`}>
              <span class="cs-research-num mono" style={`color: ${project.accent}`}>{String(i + 1).padStart(2, '0')}</span>
              <span class="cs-research-label">{m.label}</span>
              <span class="cs-research-desc mono">{m.desc}</span>
            </div>
          ))}
        </div>

        <div class="cs-insights">
          <div class="cs-insights-header">
            <span class="mono">KEY INSIGHTS *</span>
          </div>
          {project.insights.map((insight, i) => (
            <div class="cs-insight-row">
              <span class="cs-insight-num mono" style={`color: ${project.accent}`}>{String(i + 1).padStart(2, '0')}</span>
              <span class="cs-insight-text">{insight}</span>
            </div>
          ))}
        </div>
      </section>

      <!--  DESIGN PROCESS  -->
      <section class="cs-section" id="cs-process">
        <h2 class="cs-section-label mono">DESIGN PROCESS</h2>
        <div class="cs-process-track">
          {project.processSteps.map((step, i) => (
            <div class="cs-process-step" style={`--accent: ${project.accent}`}>
              <div class="cs-ps-num mono">{step.num}</div>
              <div class="cs-ps-content">
                <div class="cs-ps-title">{step.title}</div>
                <div class="cs-ps-desc mono">{step.desc}</div>
              </div>
              {i < project.processSteps.length - 1 && (
                <div class="cs-ps-arrow">-></div>
              )}
            </div>
          ))}
        </div>
      </section>

      <!--  SOLUTION WALKTHROUGH  -->
      <section class="cs-section" id="cs-screens">
        <h2 class="cs-section-label mono">SOLUTION WALKTHROUGH</h2>
        <div class="cs-solution-grid">
          {project.screens.map((screen, i) => (
            <article class="cs-solution-step" style={`--accent: ${project.accent}`}>
              <span class="cs-solution-index mono">{String(i + 1).padStart(2, '0')}</span>
              <div>
                <span class="cs-solution-kicker mono">DESIGN MOMENT</span>
                <h3 class="cs-screen-title">{screen.title}</h3>
                <p class="cs-screen-desc">{screen.desc}</p>
              </div>
            </article>
          ))}
        </div>
      </section>

      <!--  DESIGN DECISIONS  -->
      <section class="cs-section" id="cs-decisions">
        <h2 class="cs-section-label mono">KEY DESIGN DECISIONS</h2>
        <div class="cs-decisions-list">
          {project.decisions.map((d, i) => (
            <div class="cs-decision-row" style={`--accent: ${project.accent}`}>
              <div class="cs-decision-left">
                <span class="cs-decision-num mono">{String(i + 1).padStart(2,'0')}</span>
                <span class="cs-decision-label">DECISION</span>
                <p class="cs-decision-text">{d.decision}</p>
              </div>
              <div class="cs-decision-arrow">-></div>
              <div class="cs-decision-right">
                <span class="cs-decision-label">RATIONALE</span>
                <p class="cs-decision-text">{d.rationale}</p>
              </div>
            </div>
          ))}
        </div>
      </section>

      <!--  OUTCOMES  -->
      <section class="cs-section" id="cs-outcomes">
        <h2 class="cs-section-label mono">OUTCOMES & IMPACT</h2>
        <div class="cs-outcomes-grid">
          {project.outcomes.map(o => (
            <div class="cs-outcome-card" style={`--accent: ${project.accent}`}>
              <span class="cs-outcome-metric">{o.metric}</span>
              <span class="cs-outcome-label mono">{o.label}</span>
            </div>
          ))}
        </div>
      </section>

      <!--  LEARNINGS  -->
      <section class="cs-section" id="cs-learnings">
        <h2 class="cs-section-label mono">LEARNINGS & REFLECTION</h2>
        <div class="cs-learnings-grid">
          <div class="cs-learning-col cs-learning-worked">
            <div class="cs-learning-header" style={`background: ${project.accent}`}>
              <span class="mono">* WHAT WORKED</span>
            </div>
            <ul class="cs-learning-list">
              {project.learnings.worked.map(item => (
                <li>{item}</li>
              ))}
            </ul>
          </div>
          <div class="cs-learning-col cs-learning-improve">
            <div class="cs-learning-header">
              <span class="mono">* WHAT I'D IMPROVE</span>
            </div>
            <ul class="cs-learning-list">
              {project.learnings.improve.map(item => (
                <li>{item}</li>
              ))}
            </ul>
          </div>
        </div>
      </section>

    </main>

    <!--  PROJECT NAVIGATION  -->
    <nav class="cs-project-nav" id="cs-project-nav">
      <div class="cs-container">
        <div class="cs-pnav-inner">
          {prevProject ? (
            <a href={`/projects/${prevProject.slug}`} class="cs-pnav-card cs-pnav-prev">
              <span class="cs-pnav-dir mono">Previous work</span>
              <span class="cs-pnav-title">{prevProject.title}</span>
              <span class="cs-pnav-type mono">{prevProject.type}</span>
            </a>
          ) : <div />}

          <a href="/#projects" class="cs-pnav-all mono">ALL WORKS -></a>

          {nextProject ? (
            <a href={`/projects/${nextProject.slug}`} class="cs-pnav-card cs-pnav-next">
              <span class="cs-pnav-dir mono">NEXT -></span>
              <span class="cs-pnav-title">{nextProject.title}</span>
              <span class="cs-pnav-type mono">{nextProject.type}</span>
            </a>
          ) : <div />}
        </div>
      </div>
    </nav>

  </div>
</Layout>

<style>
  /*  Base  */
  .case-study {
    background: var(--bg);
    min-height: 100vh;
  }

  .cs-container {
    max-width: 1100px;
    margin: 0 auto;
    padding: 0 24px;
  }

  .cs-body {
    padding-top: 0;
    padding-bottom: 80px;
  }

  .cs-section {
    padding: 64px 0;
    border-bottom: var(--border-thin);
  }

  .cs-section:last-child { border-bottom: none; }

  .cs-section-label {
    display: inline-block;
    font-size: var(--text-xs);
    font-weight: 700;
    letter-spacing: 3px;
    color: var(--grey);
    margin-bottom: 32px;
    padding: 6px 14px;
    border: var(--border-thick);
    background: var(--cream);
  }

  /*  Top Nav  */
  .cs-topnav {
    position: sticky;
    top: 0;
    z-index: 100;
    background: var(--bg);
    border-bottom: var(--border-thick);
    padding: 14px 0;
    box-shadow: 0 4px 0 var(--primary);
  }

  .cs-topnav-inner {
    max-width: 1100px;
    margin: 0 auto;
    padding: 0 24px;
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  .cs-back {
    font-family: var(--font-mono);
    font-size: var(--text-xs);
    font-weight: 700;
    letter-spacing: 1px;
    color: var(--black);
    padding: 8px 16px;
    border: var(--border-thick);
    background: var(--primary);
    box-shadow: var(--shadow-sm);
    transition: all 0.15s ease;
  }

  .cs-back:hover {
    box-shadow: none;
    transform: none;
  }

  .cs-breadcrumb {
    font-size: var(--text-xs);
    color: var(--grey);
    letter-spacing: 1px;
  }

  .cs-breadcrumb a {
    color: var(--grey);
    transition: color 0.15s ease;
  }

  .cs-breadcrumb a:hover { color: var(--secondary); }

  /*  Hero  */
  .cs-hero {
    padding: 72px 0 48px;
    border-bottom: var(--border-brutal);
  }

  .cs-type-badge {
    display: inline-block;
    font-size: var(--text-xs);
    font-weight: 700;
    letter-spacing: 2px;
    text-transform: uppercase;
    padding: 6px 14px;
    background: var(--accent);
    border: var(--border-thick);
    box-shadow: var(--shadow-sm);
    margin-bottom: 24px;
    color: var(--black);
  }

  .cs-title {
    font-family: var(--font-serif);
    font-size: clamp(2.8rem, 6vw, 5rem);
    font-weight: 700;
    line-height: 1.05;
    color: var(--black);
    margin-bottom: 20px;
    letter-spacing: -0.02em;
  }

  .cs-tagline {
    font-size: var(--text-lg);
    color: var(--dark-grey);
    line-height: 1.6;
    max-width: 640px;
    margin-bottom: 40px;
  }

  .cs-meta-strip {
    display: flex;
    flex-wrap: wrap;
    gap: 0;
    border: var(--border-brutal);
    box-shadow: var(--shadow-sm);
    width: fit-content;
  }

  .cs-meta-item {
    display: flex;
    flex-direction: column;
    gap: 4px;
    padding: 16px 24px;
    border-right: var(--border-thick);
    background: var(--white);
  }

  .cs-meta-item:last-child { border-right: none; }

  .cmi-label {
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 2px;
    color: var(--grey);
    text-transform: uppercase;
  }

  .cmi-val {
    font-size: var(--text-sm);
    font-weight: 600;
    color: var(--black);
  }

  /*  Cover Visual  */
  .cs-hero-visual {
    padding: 40px 0;
    background: var(--cream);
    border-bottom: var(--border-brutal);
  }

  .cs-cover-placeholder {
    border: var(--border-brutal);
    box-shadow: var(--shadow-lg);
    overflow: hidden;
    background: var(--white);
  }

  .cs-cover-browser {
    width: 100%;
  }

  .cs-cover-bar {
    display: flex;
    align-items: center;
    gap: 8px;
    padding: 10px 16px;
    background: var(--cream);
    border-bottom: var(--border-thick);
  }

  .dot {
    width: 12px;
    height: 12px;
    border-radius: 50%;
    border: 1px solid rgba(0,0,0,0.2);
    flex-shrink: 0;
  }

  .dot.red    { background: #ef4444; }
  .dot.yellow { background: #eab308; }
  .dot.green  { background: #22c55e; }

  .cs-cover-url {
    font-size: 11px;
    color: var(--grey);
    letter-spacing: 0.5px;
    margin-left: 8px;
  }

  .cs-cover-screen {
    height: 380px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: repeating-linear-gradient(
      45deg,
      var(--cream) 0px,
      var(--cream) 10px,
      var(--bg) 10px,
      var(--bg) 20px
    );
  }

  .cs-cover-label {
    font-size: var(--text-sm);
    font-weight: 700;
    letter-spacing: 3px;
    color: var(--grey);
    text-align: center;
    line-height: 2;
    padding: 24px 40px;
    background: var(--white);
    border: var(--border-brutal);
    box-shadow: var(--shadow-md);
  }

  .cs-cover-screen.has-banner {
    background: none;
    height: auto;
    min-height: 380px;
  }

  .cs-cover-banner-img {
    width: 100%;
    height: 100%;
    object-fit: contain;
    background: #0b0d0f;
    display: block;
    transition: transform 0.5s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .cs-cover-placeholder:hover .cs-cover-banner-img {
    transform: scale(1.03);
  }

  /*  Overview  */
  .cs-overview-grid {
    display: grid;
    grid-template-columns: 1.4fr 0.6fr;
    gap: 48px;
    align-items: start;
  }

  .cs-overview-text {
    font-size: var(--text-lg);
    line-height: 1.7;
    color: var(--dark-grey);
  }

  .cs-stat-hero {
    border: var(--border-brutal);
    box-shadow: var(--shadow-primary);
    padding: 32px 24px;
    text-align: center;
    background: var(--white);
    margin-bottom: 16px;
    transition: all 0.15s ease;
  }

  .cs-stat-hero:hover {
    box-shadow: none;
    transform: none;
  }

  .cs-stat-num {
    display: block;
    font-size: clamp(3rem, 6vw, 4.5rem);
    font-weight: 900;
    color: var(--black);
    line-height: 1;
    letter-spacing: -0.04em;
    margin-bottom: 8px;
  }

  .cs-stat-label {
    font-size: var(--text-xs);
    font-weight: 700;
    letter-spacing: 2px;
    color: var(--grey);
    text-transform: uppercase;
  }

  .cs-team-card {
    border: var(--border-thick);
    padding: 16px 20px;
    display: flex;
    flex-direction: column;
    gap: 6px;
    background: var(--white);
  }

  .cs-team-val {
    font-size: var(--text-sm);
    font-weight: 600;
    color: var(--text);
  }

  /*  Challenge  */
  .cs-challenge-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 48px;
    align-items: start;
  }

  .cs-challenge-text {
    font-size: var(--text-base);
    line-height: 1.8;
    color: var(--dark-grey);
  }

  .cs-pain-list {
    border: var(--border-brutal);
    box-shadow: var(--shadow-sm);
    overflow: hidden;
  }

  .cs-pain-header {
    padding: 12px 20px;
    background: var(--black);
    color: var(--primary);
    font-size: var(--text-xs);
    font-weight: 700;
    letter-spacing: 2px;
    border-bottom: var(--border-thick);
  }

  .cs-pain-item {
    display: flex;
    align-items: flex-start;
    gap: 16px;
    padding: 18px 20px;
    border-bottom: var(--border-thin);
    background: var(--white);
    transition: all 0.15s ease;
  }

  .cs-pain-item:last-child { border-bottom: none; }

  .cs-pain-item:hover {
    background: var(--cream);
    padding-left: 20px;
  }

  .cs-pain-num {
    font-size: var(--text-xs);
    font-weight: 700;
    color: var(--secondary);
    flex-shrink: 0;
    margin-top: 2px;
  }

  .cs-pain-text {
    font-size: var(--text-sm);
    font-weight: 500;
    color: var(--text);
    line-height: 1.5;
  }

  /*  Research  */
  .cs-research-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 0;
    border: var(--border-brutal);
    box-shadow: var(--shadow-sm);
    margin-bottom: 40px;
  }

  .cs-research-card {
    padding: 28px 20px;
    border-right: var(--border-thick);
    background: var(--white);
    display: flex;
    flex-direction: column;
    gap: 8px;
    transition: all 0.15s ease;
  }

  .cs-research-card:last-child { border-right: none; }

  .cs-research-card:hover {
    background: var(--cream);
    transform: none;
    box-shadow: var(--shadow-sm);
  }

  .cs-research-num {
    font-size: var(--text-xs);
    font-weight: 700;
  }

  .cs-research-label {
    font-size: var(--text-base);
    font-weight: 700;
    color: var(--text);
    line-height: 1.2;
  }

  .cs-research-desc {
    font-size: 11px;
    font-weight: 500;
    color: var(--grey);
    letter-spacing: 0.3px;
    line-height: 1.5;
  }

  .cs-insights {
    border: var(--border-brutal);
    box-shadow: var(--shadow-sm);
    overflow: hidden;
  }

  .cs-insights-header {
    padding: 12px 20px;
    background: var(--secondary);
    color: var(--white);
    font-family: var(--font-mono);
    font-size: var(--text-xs);
    font-weight: 700;
    letter-spacing: 2px;
    border-bottom: var(--border-thick);
  }

  .cs-insight-row {
    display: flex;
    align-items: flex-start;
    gap: 20px;
    padding: 20px 24px;
    border-bottom: var(--border-thin);
    background: var(--white);
    transition: all 0.15s ease;
  }

  .cs-insight-row:last-child { border-bottom: none; }

  .cs-insight-row:hover {
    background: var(--cream);
    padding-left: 24px;
  }

  .cs-insight-num {
    font-size: var(--text-xs);
    font-weight: 700;
    flex-shrink: 0;
    margin-top: 3px;
  }

  .cs-insight-text {
    font-size: var(--text-base);
    font-weight: 500;
    color: var(--text);
    line-height: 1.6;
  }

  /*  Process  */
  .cs-process-track {
    display: flex;
    align-items: stretch;
    gap: 0;
    border: var(--border-brutal);
    box-shadow: var(--shadow-sm);
    overflow: hidden;
  }

  .cs-process-step {
    flex: 1;
    display: flex;
    flex-direction: column;
    gap: 12px;
    padding: 32px 24px;
    border-right: var(--border-thick);
    background: var(--white);
    position: relative;
    transition: all 0.15s ease;
  }

  .cs-process-step:last-child { border-right: none; }

  .cs-process-step:hover {
    background: var(--accent, var(--primary));
  }

  .cs-ps-num {
    font-size: var(--text-xs);
    font-weight: 700;
    letter-spacing: 2px;
    color: var(--secondary);
  }

  .cs-ps-title {
    font-size: var(--text-lg);
    font-weight: 700;
    color: var(--text);
  }

  .cs-ps-desc {
    font-size: 11px;
    font-weight: 500;
    color: var(--grey);
    letter-spacing: 0.3px;
    line-height: 1.6;
  }

  .cs-ps-arrow {
    display: none;
  }

  /*  Screens  */
  .cs-screens-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 24px;
  }

  .cs-screen-card {
    border: var(--border-brutal);
    box-shadow: var(--shadow-sm);
    overflow: hidden;
    background: var(--white);
    transition: all 0.15s ease;
  }

  .cs-screen-card:hover {
    box-shadow: none;
    transform: none;
  }

  .cs-screen-placeholder {
    border-bottom: var(--border-thick);
  }

  .cs-screen-bar {
    display: flex;
    align-items: center;
    gap: 6px;
    padding: 8px 12px;
    background: var(--cream);
    border-bottom: var(--border-thin);
  }

  .cs-screen-bar .dot {
    width: 8px;
    height: 8px;
  }

  .cs-screen-area {
    height: 180px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: repeating-linear-gradient(
      -45deg,
      var(--cream) 0px,
      var(--cream) 8px,
      var(--bg) 8px,
      var(--bg) 16px
    );
  }

  .cs-screen-area span {
    font-size: var(--text-xs);
    font-weight: 700;
    letter-spacing: 2px;
    color: var(--grey);
    padding: 10px 16px;
    background: var(--white);
    border: var(--border-thin);
  }

  .cs-screen-info {
    padding: 20px;
  }

  .cs-screen-title {
    font-size: var(--text-base);
    font-weight: 700;
    color: var(--text);
    margin-bottom: 8px;
  }

  .cs-screen-desc {
    font-size: var(--text-sm);
    color: var(--grey);
    line-height: 1.6;
  }

  /*  Design Decisions  */
  .cs-decisions-list {
    display: flex;
    flex-direction: column;
    gap: 0;
    border: var(--border-brutal);
    box-shadow: var(--shadow-sm);
    overflow: hidden;
  }

  .cs-decision-row {
    display: grid;
    grid-template-columns: 1fr 48px 1fr;
    border-bottom: var(--border-thick);
    background: var(--white);
    transition: background 0.15s ease;
  }

  .cs-decision-row:last-child { border-bottom: none; }

  .cs-decision-row:hover { background: var(--cream); }

  .cs-decision-left,
  .cs-decision-right {
    padding: 28px 24px;
    display: flex;
    flex-direction: column;
    gap: 8px;
  }

  .cs-decision-left {
    border-right: var(--border-thin);
  }

  .cs-decision-arrow {
    display: flex;
    align-items: center;
    justify-content: center;
    font-family: var(--font-mono);
    font-size: var(--text-lg);
    font-weight: 700;
    color: var(--secondary);
    border-right: var(--border-thin);
  }

  .cs-decision-num {
    font-size: var(--text-xs);
    font-weight: 700;
    letter-spacing: 2px;
  }

  .cs-decision-label {
    font-family: var(--font-mono);
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 2px;
    color: var(--grey);
    text-transform: uppercase;
  }

  .cs-decision-text {
    font-size: var(--text-sm);
    font-weight: 500;
    color: var(--text);
    line-height: 1.6;
  }

  /*  Outcomes  */
  .cs-outcomes-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 0;
    border: var(--border-brutal);
    box-shadow: var(--shadow-lg);
  }

  .cs-outcome-card {
    padding: 40px 24px;
    border-right: var(--border-thick);
    background: var(--white);
    text-align: center;
    display: flex;
    flex-direction: column;
    gap: 10px;
    transition: all 0.15s ease;
  }

  .cs-outcome-card:last-child { border-right: none; }

  .cs-outcome-card:hover {
    background: var(--accent, var(--primary));
    transform: none;
    box-shadow: var(--shadow-sm);
  }

  .cs-outcome-metric {
    font-size: clamp(2.5rem, 5vw, 3.5rem);
    font-weight: 900;
    color: var(--black);
    line-height: 1;
    letter-spacing: -0.04em;
  }

  .cs-outcome-label {
    font-size: var(--text-xs);
    font-weight: 700;
    letter-spacing: 1.5px;
    color: var(--grey);
    text-transform: uppercase;
    line-height: 1.4;
  }

  /*  Learnings  */
  .cs-learnings-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 0;
    border: var(--border-brutal);
    box-shadow: var(--shadow-sm);
    overflow: hidden;
  }

  .cs-learning-col {
    background: var(--white);
  }

  .cs-learning-col:first-child {
    border-right: var(--border-thick);
  }

  .cs-learning-header {
    padding: 14px 24px;
    background: var(--cream);
    border-bottom: var(--border-thick);
    font-family: var(--font-mono);
    font-size: var(--text-xs);
    font-weight: 700;
    letter-spacing: 2px;
    color: var(--black);
  }

  .cs-learning-worked .cs-learning-header {
    background: var(--accent, var(--primary));
  }

  .cs-learning-list {
    list-style: none;
    padding: 0;
  }

  .cs-learning-list li {
    padding: 18px 24px;
    border-bottom: var(--border-thin);
    font-size: var(--text-sm);
    color: var(--text);
    line-height: 1.6;
    padding-left: 40px;
    position: relative;
    transition: all 0.15s ease;
  }

  .cs-learning-list li:last-child { border-bottom: none; }

  .cs-learning-list li::before {
    content: '*';
    position: absolute;
    left: 20px;
    color: var(--secondary);
    font-size: 10px;
    top: 21px;
  }

  .cs-learning-list li:hover {
    background: var(--cream);
    padding-left: 40px;
  }

  /*  Project Navigation  */
  .cs-project-nav {
    border-top: var(--border-brutal);
    padding: 48px 0;
    background: var(--cream);
  }

  .cs-pnav-inner {
    display: grid;
    grid-template-columns: 1fr auto 1fr;
    gap: 24px;
    align-items: center;
  }

  .cs-pnav-card {
    border: var(--border-brutal);
    box-shadow: var(--shadow-sm);
    padding: 24px;
    background: var(--white);
    display: flex;
    flex-direction: column;
    gap: 6px;
    transition: all 0.15s ease;
  }

  .cs-pnav-card:hover {
    box-shadow: none;
    transform: none;
    background: var(--primary);
  }

  .cs-pnav-next { text-align: right; }

  .cs-pnav-dir {
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 2px;
    color: var(--grey);
    text-transform: uppercase;
  }

  .cs-pnav-title {
    font-size: var(--text-lg);
    font-weight: 700;
    color: var(--text);
  }

  .cs-pnav-type {
    font-size: 11px;
    font-weight: 600;
    letter-spacing: 1px;
    color: var(--grey);
  }

  .cs-pnav-all {
    font-size: var(--text-xs);
    font-weight: 700;
    letter-spacing: 2px;
    text-transform: uppercase;
    color: var(--black);
    padding: 14px 20px;
    border: var(--border-brutal);
    background: var(--primary);
    box-shadow: var(--shadow-sm);
    white-space: nowrap;
    text-align: center;
    transition: all 0.15s ease;
  }

  .cs-pnav-all:hover {
    box-shadow: none;
    transform: none;
  }

  /*  Responsive  */
  @media (max-width: 1024px) {
    .cs-overview-grid    { grid-template-columns: 1fr; }
    .cs-challenge-grid   { grid-template-columns: 1fr; }
    .cs-research-grid    { grid-template-columns: repeat(2, 1fr); }
    .cs-research-card:nth-child(2) { border-right: none; }
    .cs-research-card:nth-child(1),
    .cs-research-card:nth-child(2) { border-bottom: var(--border-thick); }
    .cs-screens-grid     { grid-template-columns: 1fr 1fr; }
    .cs-process-track    { flex-direction: column; }
    .cs-process-step     { border-right: none; border-bottom: var(--border-thick); }
    .cs-process-step:last-child { border-bottom: none; }
    .cs-meta-strip       { flex-wrap: wrap; }
    .cs-meta-item        { border-bottom: var(--border-thin); }
  }

  @media (max-width: 768px) {
    .cs-title            { font-size: clamp(2rem, 8vw, 3rem); }
    .cs-screens-grid     { grid-template-columns: 1fr; }
    .cs-outcomes-grid    { grid-template-columns: 1fr; }
    .cs-outcome-card     { border-right: none; border-bottom: var(--border-thick); }
    .cs-outcome-card:last-child { border-bottom: none; }
    .cs-learnings-grid   { grid-template-columns: 1fr; }
    .cs-learning-col:first-child { border-right: none; border-bottom: var(--border-thick); }
    .cs-decision-row     { grid-template-columns: 1fr; }
    .cs-decision-left    { border-right: none; border-bottom: var(--border-thin); }
    .cs-decision-arrow   { display: none; }
    .cs-pnav-inner       { grid-template-columns: 1fr; }
    .cs-pnav-all         { order: -1; }
    .cs-breadcrumb       { display: none; }
  }
  .cs-skip { position: fixed; left: 20px; top: -80px; z-index: 200; padding: 12px 16px; background: var(--primary); color: var(--black); font-family: var(--font-mono); font-size: 12px; font-weight: 700; border: var(--border-thick); }
  .cs-skip:focus { top: 16px; }
  .cs-executive-summary { display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); width: min(100%, 860px); border: var(--border-brutal); margin-top: 24px; }
  .cs-executive-summary > div { display: grid; gap: 7px; padding: 16px 18px; background: var(--white); border-right: var(--border-thick); }
  .cs-executive-summary > div:last-child { border-right: 0; }
  .cs-executive-summary span { font-size: 10px; font-weight: 700; letter-spacing: 1.5px; color: var(--grey); }
  .cs-executive-summary strong { font-size: var(--text-sm); line-height: 1.35; color: var(--text); }
  .cs-case-toc { position: sticky; top: 61px; z-index: 90; background: color-mix(in srgb, var(--bg) 94%, transparent); backdrop-filter: blur(10px); border-bottom: var(--border-thin); }
  .cs-case-toc .cs-container { display: flex; gap: 4px; overflow-x: auto; scrollbar-width: thin; }
  .cs-case-toc a { flex: 0 0 auto; padding: 13px 11px; font-family: var(--font-mono); font-size: 10px; font-weight: 700; letter-spacing: 1px; text-transform: uppercase; color: var(--grey); }
  .cs-case-toc a:hover, .cs-case-toc a:focus-visible { color: var(--text); background: var(--primary); outline: none; }
  .cs-solution-grid { display: grid; grid-template-columns: repeat(3, 1fr); border: var(--border-brutal); }
  .cs-solution-step { display: grid; grid-template-columns: auto 1fr; gap: 18px; padding: 28px 22px; background: var(--white); border-right: var(--border-thick); min-height: 210px; }
  .cs-solution-step:last-child { border-right: 0; }
  .cs-solution-step:hover { background: var(--cream); }
  .cs-solution-index { color: var(--secondary); font-size: var(--text-xs); font-weight: 700; }
  .cs-solution-kicker { display: block; color: var(--grey); font-size: 10px; font-weight: 700; letter-spacing: 1.5px; margin-bottom: 14px; }
  @media (max-width: 1024px) { .cs-solution-grid { grid-template-columns: 1fr 1fr; } .cs-solution-step:nth-child(2n) { border-right: 0; } .cs-solution-step:nth-child(n + 3) { border-top: var(--border-thick); } }
  @media (max-width: 768px) { .cs-executive-summary { grid-template-columns: 1fr; } .cs-executive-summary > div { border-right: 0; border-bottom: var(--border-thin); } .cs-executive-summary > div:last-child { border-bottom: 0; } .cs-case-toc { top: 56px; } .cs-solution-grid { grid-template-columns: 1fr; } .cs-solution-step, .cs-solution-step:nth-child(2n) { border-right: 0; border-top: var(--border-thick); } .cs-solution-step:first-child { border-top: 0; } }

  /* Case studies use the same dark fieldbook system as the homepage. */
  .case-study { background: var(--bg); color: var(--text); }
  .cs-container { max-width: var(--max-width); padding-inline: clamp(20px, 4vw, 56px); }
  .cs-topnav { background: rgba(16,18,20,.94); border-bottom: var(--border-thin); box-shadow: 0 1px 0 var(--lime-20); padding: 18px 0; }
  .cs-topnav-inner { max-width: var(--max-width); padding-inline: clamp(20px, 4vw, 56px); }
  .cs-back { padding: 0; border: 0; box-shadow: none; background: transparent; color: var(--primary); text-transform: uppercase; }
  .cs-back::before { content: '<- '; color: var(--secondary); }
  .cs-back:hover, .cs-pnav-all:hover { box-shadow: none; transform: none; color: var(--text); }
  .cs-hero { padding: clamp(80px, 11vw, 152px) 0 56px; border-bottom: var(--border-thin); position: relative; overflow: hidden; }
  .cs-hero::after { content: 'CASE STUDY'; position: absolute; right: -.06em; bottom: -.32em; font: 900 clamp(8rem,20vw,22rem)/.8 var(--font-display); letter-spacing: -.06em; color: rgba(183,243,91,.035); pointer-events: none; }
  .cs-hero .cs-container { position: relative; z-index: 1; }
  .cs-type-badge { padding: 0; margin-bottom: 22px; border: 0; box-shadow: none; background: transparent; color: var(--primary); font: 700 var(--text-xs)/1 var(--font-mono); letter-spacing: 2px; }
  .cs-type-badge::before, .cs-section-label::before { content: '// '; color: var(--secondary); }
  .cs-title { max-width: 1040px; font-family: var(--font-display); font-size: clamp(4.2rem,11vw,11rem); font-weight: 400; line-height: .82; letter-spacing: .01em; text-transform: uppercase; color: var(--text); margin-bottom: 28px; }
  .cs-tagline { max-width: 650px; margin-bottom: 40px; color: var(--dark-grey); font-size: clamp(1rem,1.6vw,1.25rem); }
  .cs-meta-strip { width: 100%; display: grid; grid-template-columns: repeat(5,minmax(0,1fr)); border: 0; border-top: var(--border-thin); border-bottom: var(--border-thin); box-shadow: none; }
  .cs-meta-item { min-height: 82px; padding: 14px 15px; border-right: var(--border-thin); border-bottom: 0; background: transparent; }
  .cs-meta-item:first-child { padding-left: 0; } .cs-meta-item:last-child { border-right: 0; }
  .cmi-label, .cs-executive-summary span { color: var(--grey); } .cmi-val { color: var(--text); font-size: var(--text-sm); }
  .cs-executive-summary { width: 100%; margin-top: 0; border: 0; border-bottom: var(--border-thin); }
  .cs-executive-summary > div { padding: 16px 15px; background: transparent; border-right: var(--border-thin); }
  .cs-executive-summary > div:first-child { padding-left: 0; } .cs-executive-summary > div:last-child { border-right: 0; } .cs-executive-summary strong { color: var(--text); }
  .cs-hero-visual { padding: clamp(28px,5vw,56px) 0; background: #0c0f11; border-bottom: var(--border-thin); }
  .cs-cover-placeholder { border: var(--border-thin); box-shadow: none; background: #0b0d0f; } .cs-cover-bar { background: #15191c; border-bottom: var(--border-thin); }
  .cs-cover-screen.has-banner { min-height: clamp(230px,38vw,500px); } .cs-cover-placeholder:hover .cs-cover-banner-img { transform: scale(1.015); }
  .cs-case-toc { top: 65px; background: rgba(16,18,20,.96); border-bottom: var(--border-thin); } .cs-case-toc a { padding: 16px 12px; color: var(--grey); }
  .cs-case-toc a:hover, .cs-case-toc a:focus-visible { color: var(--primary); background: transparent; box-shadow: inset 0 -2px var(--primary); }
  .cs-body { padding-top: 0; padding-bottom: clamp(60px,9vw,120px); } .cs-section { padding: clamp(70px,9vw,120px) 0; border-bottom: var(--border-thin); scroll-margin-top: 110px; }
  .cs-section-label { display: block; width: fit-content; padding: 0; margin: 0 0 32px; border: 0; background: none; color: var(--primary); font-size: var(--text-xs); }
  .cs-overview-grid, .cs-challenge-grid { gap: clamp(32px,6vw,84px); } .cs-overview-text, .cs-challenge-text { color: var(--text); font-size: clamp(1.2rem,2.2vw,1.75rem); line-height: 1.5; }
  .cs-stat-hero, .cs-team-card, .cs-pain-list, .cs-research-grid, .cs-insights, .cs-process-track, .cs-solution-grid, .cs-decisions-list, .cs-outcomes-grid, .cs-learnings-grid { border-color: rgba(183,243,91,.18); box-shadow: none; }
  .cs-stat-hero, .cs-team-card, .cs-pain-item, .cs-research-card, .cs-insight-row, .cs-process-step, .cs-solution-step, .cs-decision-row, .cs-outcome-card, .cs-learning-col, .cs-pnav-card { background: var(--white); }
  .cs-stat-hero { text-align: left; padding: 26px; } .cs-stat-hero:hover, .cs-pnav-card:hover, .cs-pnav-all:hover { transform: none; box-shadow: none; }
  .cs-pain-header, .cs-insights-header, .cs-learning-header { background: #15191c; color: var(--primary); border-bottom: var(--border-thin); }
  .cs-pain-item:hover, .cs-insight-row:hover, .cs-research-card:hover, .cs-solution-step:hover, .cs-decision-row:hover, .cs-learning-list li:hover, .cs-process-step:hover, .cs-outcome-card:hover, .cs-pnav-card:hover { background: var(--cream); transform: none; box-shadow: none; }
  .cs-ps-title, .cs-screen-title, .cs-pnav-title { color: var(--text); } .cs-decision-arrow { color: var(--primary); } .cs-learning-worked .cs-learning-header { background: var(--primary); color: var(--on-primary); }
  .cs-project-nav { padding: clamp(60px,8vw,100px) 0; background: #0c0f11; border-top: var(--border-thin); } .cs-pnav-card { border-color: rgba(183,243,91,.2); box-shadow: none; } .cs-pnav-all { border: 0; box-shadow: none; background: transparent; color: var(--primary); padding: 14px 0; }
  @media (max-width:1024px) { .cs-meta-strip { grid-template-columns: repeat(3,1fr); } .cs-meta-item:nth-child(3) { border-right: 0; } .cs-meta-item:nth-child(-n+3) { border-bottom: var(--border-thin); } }
  @media (max-width:768px) { .cs-topnav { padding: 15px 0; } .cs-hero { padding-top: 90px; } .cs-title { font-size: clamp(4.1rem,22vw,7rem); } .cs-meta-strip { grid-template-columns: 1fr 1fr; } .cs-meta-item { border-bottom: var(--border-thin); } .cs-meta-item:nth-child(odd) { border-right: var(--border-thin); } .cs-meta-item:nth-child(even) { border-right: 0; } .cs-meta-item:last-child { grid-column: 1 / -1; } .cs-case-toc { top: 57px; } }

  /* Shared soft-card treatment for every public case study. */
  .cs-cover-placeholder, .cs-stat-hero, .cs-team-card, .cs-pain-list, .cs-research-grid, .cs-insights, .cs-process-track, .cs-solution-grid, .cs-decisions-list, .cs-outcomes-grid, .cs-learnings-grid, .cs-pnav-card { border-radius: 16px; overflow: hidden; }
  .cs-executive-summary { border-radius: 14px; overflow: hidden; }
  .cs-case-toc { border-radius: 0 0 14px 14px; }
  .cs-type-badge, .cs-section-label { border-radius: 999px; }

</style>
